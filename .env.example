# qual-match 环境变量示例
# 拷贝本文件为 .env.local（已被 git 忽略），按需填值。
# 凭据键名约定：<SOURCE>_USERNAME / <SOURCE>_PASSWORD（绝不写进代码/文档/提交）。

# 后端监听端口（默认 3000）
# PORT=3000

# —— 抓取源凭据（阶段 4/5 接入时才需要）——
# PROV_CMA_USERNAME=
# PROV_CMA_PASSWORD=
